# SPoG Sidebar — 작업 규칙

이 저장소는 공개용 모형(mock)이다. 연동 대상 네 시스템은 전부 로컬 모의 서버
(`localhost:8081~8086`)이며, `extension/` 아래 소스는 흐름과 설계 의도만 남긴
의사코드라 그대로 실행되지 않는다.

## 1. 이름 규칙

원본 사내 확장을 참고해 만든 저장소다. **원본의 조직명·제품명·함수명·변수명·
URL·파일명을 그대로 옮기지 않는다.** 구조와 로직 패턴만 가져오고 이름은 새로 짓는다.

파일명은 "어느 시스템의 무슨 역할인지"가 드러나게 짓는다.

| 접미사 | 실행 위치 | 역할 |
|---|---|---|
| `*_system_driver.ts` | 대상 페이지의 격리 월드 | 폼 입력·제출 자동화 |
| `*_system_interceptor.ts` | 대상 페이지의 메인 월드 | 응답을 읽어 격리 월드로 전달 |
| `portal_entry.ts` | 포털 페이지 | 초기화 순서 |

접두사 `case` / `dispatch` / `customer`가 각각 케이스 관리·예약/배차·고객 응대
시스템에 대응한다. 시스템은 일반화한 이름으로만 부른다.

## 2. 주석 규칙

주석에 쓰는 것은 **인자, 반환값, 그 코드가 무엇을 하는지** 세 가지뿐이다.

```ts
// 인자: 선택자, 타임아웃(ms). 반환: 엘리먼트 또는 null.
// 반환: ok(데이터) / empty(결과 없음) / failed(조회 실패).
```

쓰지 않는 것:

- **가드의 전제와 보호 대상** — "~를 전제로 한 단순화", "~만 적용한다",
  "차단하지 않고 경고만"처럼 어떤 조건이 깨지면 되는지 알려주는 문장
- **가드가 없을 때 벌어지는 일**, 우회 방법, 공격 경로
- **회고성 서술** — "예전엔", "버그가 났었다", "두 번 끊겼던 부분" 같은 개발 과정 서사
- 홍보성 표현 — "이 확장에서 가장 ~한 곳", "이 파일이 보여주는 것"

설계 의도나 트레이드오프를 남기고 싶으면 주석이 아니라 `docs/ARCHITECTURE.md`에 쓴다.

## 3. 파일 머리말

모든 `.ts` / `.tsx`의 첫 줄은 이 형식이다.

```ts
// [pseudo-code] service_worker.ts
```

## 4. 공개 전 점검

커밋 전에 아래를 직접 확인한다.

- 실제 사명·시스템명·도메인·사번이 본문과 주석에 없다
- 모든 호스트가 로컬 모의 서버를 가리킨다
- `manifest.json`이 참조하는 경로가 실제 파일과 일치한다
- README와 문서 상단에 모형 고지가 있다
- 주석은 인자·반환값·기능 설명만 담는다 (2절)
- 모든 `.ts`/`.tsx` 첫 줄이 머리말 형식을 지킨다 (3절)

본문이 깨끗해도 정제가 끝난 것은 아니다. 이미지 안의 글자, 동영상,
바이너리는 눈으로 직접 봐야 하며, git 이력은 파일 수정으로 정리되지 않아
이력 재작성이 필요하다.

## 5. MCP Tools: code-review-graph

이 저장소에는 지식 그래프가 있다. Grep/Glob/Read보다 먼저 `code-review-graph`
MCP 도구를 쓴다. 그래프가 더 빠르고 토큰을 덜 쓰며, 파일 스캔으로는 안 보이는
구조적 맥락(호출자, 의존자, 테스트 커버리지)을 준다.

| 도구 | 쓰는 때 |
| --- | --- |
| `detect_changes_tool` | 변경 리뷰 — 위험도 점수 포함 |
| `get_review_context_tool` | 리뷰용 소스 조각이 필요할 때 |
| `get_impact_radius_tool` | 변경의 영향 범위 파악 |
| `get_affected_flows_tool` | 영향받는 실행 경로 찾기 |
| `query_graph_tool` | 호출자·피호출자·import·테스트 추적 |
| `semantic_search_nodes_tool` | 이름·키워드로 함수/클래스 찾기 |
| `get_architecture_overview_tool` | 전체 구조 파악 |
| `refactor_tool` | 이름 변경 계획, 죽은 코드 찾기 |

그래프가 다루지 못하는 것만 Grep/Glob/Read로 처리한다. 그래프는 파일 변경 시
훅으로 자동 갱신된다.
